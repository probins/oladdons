// html wrapped in export so can be imported like code

export default `
<template id="cursorpositiontemplate">
  <style>
    .llcursor {
      top: 8px;
      right: 8px;
      position: absolute;
    }
    .projcursor {
      top: 28px;
    }
  </style>
</template>
`;